#Mongo Database using Python & Flask
Database query endpoints using Flask and Python.

##Configuration
Local config using virtualenv.

```pip install virtualenv```

Next

```virtualenv -p python2.7 venv```
```source venv/bin/activate/```

Then
```python file_name.py```
